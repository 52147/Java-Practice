// Multiple catch Blocks:

// Allows handling different types of exceptions separately.
try {
    // Code that may throw multiple exceptions
} catch (IOException e) {
    // Handle IOException
} catch (SQLException e) {
    // Handle SQLException
}
